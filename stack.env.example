# Aperture Stack Environment Variables
# Copy this to stack.env in your repo, or set these in Portainer's environment variables section

# Timezone
TZ=Europe/London

# Image tag (use 'latest' for most recent, or specific version like '0.1.9-alpha01')
IMAGE_TAG=0.1.9-alpha01

# Domain Configuration (REQUIRED - set your own domains)
WEB_DOMAIN=aperture.example.com
API_DOMAIN=aperture-api.example.com

# Network (must match your Traefik network)
NETWORK_NAME=traefik_default

# CrowdSec Bouncer API Key
# Generate with: docker exec crowdsec cscli bouncers add aperture-bouncer
CROWDSEC_API_KEY=your-crowdsec-api-key-here

# Optional: Master key for encrypting stored API credentials
# Generate with: openssl rand -hex 32
CREDENTIALS_MASTER_KEY=
