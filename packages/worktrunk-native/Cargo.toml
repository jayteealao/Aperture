[package]
name = "worktrunk-native"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Core dependencies
napi = { version = "2", features = ["async", "tokio_rt"] }
napi-derive = "2"
git2 = "0.19"
tokio = { version = "1", features = ["rt", "rt-multi-thread"] }

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Path and string manipulation
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
strip = true
opt-level = "z"
