# Mandatory: API Token for Gateway Authentication
APERTURE_API_TOKEN=your-secure-token

# Optional: Anthropic API Key for billing (leave empty to use Subscription mode)
ANTHROPIC_API_KEY=

# Optional: Override server port
PORT=8080

# Optional: Override host
HOST=0.0.0.0
